1. Declare an environment variable `OPEN_WEATHER_API_KEY` with your own api key.
2. Just run it `sbt run`
3. Navigate your nearest browser to `localhost:8282/docs` and enjoy the swagger ui to interact with the api server.
